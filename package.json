{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "Travel3Nft",
  "scripts": {
    "build": "npm run build --workspace=Travel3Nft_frontend",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "start": "dfx start --background && npm run dev --workspace=Travel3Nft_frontend",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/Travel3Nft_frontend"
  ],
  "devDependencies": {
    "dotenv": "^16.4.7",
    "vite-plugin-environment": "^1.1.3"
  },
  "dependencies": {
    "@dfinity/identity": "^2.4.0"
  }
}
